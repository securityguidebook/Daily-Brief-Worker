{
  "name": "daily-brief-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — proxy + cron scheduler for the Daily Brief agent",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
